#include <stdio.h>
#include "main.h"

/**
 * mul - multiply two integer
 * @a: the first integer to mul
 * @b: the second  integer to mul
 * Return: addition of a and b
 */
int mul(int a, int b)
{
	return ( a * b);
}
